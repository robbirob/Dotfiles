background_opacity 1.0
linux_display_server wayland
sync_to_monitor no

# Let Hyprland own reload shortcuts and prevent kitty's resize mode from
# accidentally opening when modifier handling falls through.
map ctrl+shift+r no_op
map super+ctrl+r no_op
